{
  "name": "cat",
  "version": "0.0.3",
  "description": "simple cat action for GitHub Actions",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js && git add -f dist/"
  },
  "keywords": [
    "cat"
  ],
  "author": "Stanzilla",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.3",
    "os": "^0.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.12.2",
    "esbuild": "^0.20.2",
    "typescript": "^5.4.3"
  }
}
